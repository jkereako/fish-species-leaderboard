<h1>Hi <%= @resource.name %>!</h1>

<p>
  Someone has requested a link to change your password. You can do this through
  the link below.
</p>

<%= render 'shared/mailer/callout',
            content: "Your password won't change until you access the link below and create a new one."
            %>

<%= render 'shared/mailer/button',
            title: 'Change my password',
            url: edit_password_url(@resource, reset_password_token: @token) %>
<p>
  If you did not request a password reset, then ignore this email.
</p>
